@model CreateBarViewModel

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container pt-30">
    <div class="row gap-20 gap-lg-40">
        <div class="col-12 col-lg-7">
            <div class="content-wrapper">
                <div class="form-draft-payment">

                    <!-- Editing Bar Form start -->
                    <form asp-action="Edit" method="post">
                        <input asp-for="@Model.Bar.BarId" class="form-control" hidden />
                        <input asp-for="@Model.Bar.BarCocktailViewModels" class="form-control" hidden />
                        <input asp-for="@Model.Bar.Rating" class="form-control" hidden />
                        <h4 class="heading-title"><span>Edit Bar: "@Model.Bar.Name"</span></h4>
                        <div class="row gap-15 mb-15">
                            <div class="w-100 d-block d-md-none"></div>
                            <div class="col-6 col-md-5">
                                <div class="form-group">
                                    <label>Name</label>
                                    <input asp-for="@Model.Bar.Name" class="form-control" />
                                    <span asp-validation-for="@Model.Bar.Name" class="text-danger"></span>
                                </div>
                            </div>
                            <div class="col-6 col-md-5">
                                <div class="form-group">
                                    <label>Address</label>
                                    <input asp-for="@Model.Bar.Address" class="form-control" />
                                    <span asp-validation-for="@Model.Bar.Address" class="text-danger"></span>
                                </div>
                            </div>
                            <div class="col-6 col-md-12">
                                <div class="form-group">
                                    <label>Information</label>
                                    <input asp-for="@Model.Bar.Information" class="form-control" />
                                    <span asp-validation-for="@Model.Bar.Information" class="text-danger"></span>
                                </div>
                            </div>
                            <div class="col-6 col-sm-6 col-md-7">
                                <div class="form-group">
                                    <label>Picture</label>
                                    <input asp-for="@Model.Bar.Picture" class="form-control" />
                                    <span asp-validation-for="@Model.Bar.Picture" class="text-danger"></span>
                                </div>
                            </div>
                            <div class="col-12 col-sm-6 col-md-5">
                                <div class="form-group">
                                    <label>Map</label>
                                    <input asp-for="@Model.Bar.Map" class="form-control" />
                                    <span asp-validation-for="@Model.Bar.Map" class="text-danger"></span>
                                </div>
                            </div>
                        </div>
                        <hr class="mv-40">
                        <div class="row gap-15 mb-15">
                            <div class="col-6 col-md-12">
                                <div class="form-group chosen-bg-light">
                                    <label>Cocktails To Remove</label>
                                    <select asp-for="@Model.CocktailsToRemove" class="chosen-the-basic chosen-container-multi">
                                        @if (Model.Bar.BarCocktailViewModels.Count() > 0)
                                        {
                                            @foreach (var cocktail in Model.Bar.BarCocktailViewModels)
                                            {
                                                <option value="@cocktail.CocktailName">@cocktail.CocktailName</option>
                                            }
                                        }
                                        else
                                        {
                                            <option disabled>No cocktails found</option>
                                        }
                                    </select>
                                    <span asp-validation-for="@Model.CocktailsToRemove" class="text-danger"></span>
                                </div>
                            </div>
                        </div>
                        <hr class="mv-40">
                        <div class="row gap-15 mb-15">
                            <div class="col-6 col-md-12">
                                <div class="form-group chosen-bg-light">
                                    <label>Cocktails To Offer</label>
                                    <select asp-for="@Model.CocktailsToOffer" class="chosen-the-basic chosen-container-multi">
                                        @if (Model.CocktailsThatCanOffer.Count() > 0)
                                        {
                                            @foreach (var cocktail in Model.CocktailsThatCanOffer)
                                            {
                                                <option value="@cocktail.Name">@cocktail.Name</option>
                                            }
                                        }
                                        else
                                        {
                                            <option disabled>No cocktails found</option>
                                        }
                                    </select>
                                    <span asp-validation-for="@Model.CocktailsToOffer" class="text-danger"></span>
                                </div>
                            </div>
                        </div>
                        <div class="row mt-20">
                            <div class="col-sm-8 col-md-6">
                                <button type="submit" class="btn btn-primary">Submit Now</button>
                            </div>
                        </div>
                    </form>
                    <!-- Editing Bar Form end -->

                </div>
            </div>
        </div>

        <div class="col-12 col-lg-5">
            <!-- Side bar instruction start -->
            <aside class="sticky-kit sidebar-wrapper no-border">
                <div class="booking-box">
                    <div class="box-heading"><h3 class="h6 text-white text-uppercase">Creating Bar Instructions</h3></div>
                    <div class="box-content">
                        <ul class="border-top mt-20 pt-15">
                            <li class="clearfix"><i class="elegent-icon-check_alt2 text-success"></i> In the Select Option: "Cocktails To Remove", chose from the list which cocktails you want to REMOVE from the menu.</li>
                            <li class="clearfix"><i class="elegent-icon-check_alt2 text-success"></i> In the Slecet Option: "Cocktails To Offer", chose from the list which cocktails the bar can ADD to the menu.</li>
                            <li class="clearfix border-top font700 text-uppercase"></li>
                        </ul>
                    </div>
                    <div class="box-bottom bg-light">
                        <h6 class="font-sm">We are the best site for a night out in Sofia</h6>
                        <p class="font-sm">Our custom tour program, direct call <span class="text-primary">+66857887444</span>.</p>
                    </div>
                </div>
            </aside>
            <!-- Side bar instruction end -->
        </div>
    </div>
</div>